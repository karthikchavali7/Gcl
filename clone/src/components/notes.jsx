import React from 'react';

function Notes({ title, content }) {
  return (
    <div className="xv">
      <h1>{title}</h1>
      <p>{content}</p>
    </div>
  );
}

export default Notes; // Export the Asd component with the corrected name

